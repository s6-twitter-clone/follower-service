apiVersion : apps/v1
kind: Deployment
metadata:
  name: "twidditcluster-58fc"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "twidditcluster-58fc"
  template:
    metadata:
      labels:
        app: "twidditcluster-58fc"
    spec:
      containers:
        - name: "twidditcluster-58fc"
          image: "twiddit.azurecr.io/twidditcluster"
          ports:
          - containerPort: 80