apiVersion: v1
kind: Service
metadata:
    name: "twidditcluster-58fc"
    labels:
        app: "twidditcluster-58fc"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "twidditcluster-58fc"