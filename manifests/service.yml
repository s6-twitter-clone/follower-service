apiVersion: v1
kind: Service
metadata:
    name: "twiddit-3f49"
    labels:
        app: "twiddit-3f49"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "twiddit-3f49"